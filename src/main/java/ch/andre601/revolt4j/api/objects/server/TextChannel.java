package ch.andre601.revolt4j.api.objects.server;

/**
 * A TextChannel.
 * 
 * <p>TextChannels can contain messages of other users and also System messages such as join and leave notifications,
 * if the corresponding settings have been set.
 */
public interface TextChannel extends Channel{
}
